# Rust
/target/
Cargo.lock

# Node
frontend/node_modules/
frontend/dist/
package-lock.json

# Playwright
frontend/test-results/
frontend/playwright-report/

# WASM build output (built into frontend/src/wasm by `npm run build-wasm`)
crates/tnv-budget-tree/pkg/
frontend/src/wasm/

# wasm-pack scratch input (symlink to data/budauth.csv)
crates/tnv-budget-tree/data/

# Data — ignore everything under data/ except the two tracked files
# (local budget zips, tnv.db, .bak, store.json, etc. stay out of git).
data/*
!data/README.md
!data/budauth.csv

# Environment
.env
frontend/.env
.env.docker
.env.docker.demo

# Prebuilt docker package artifacts (large; rebuilt from source / shipped as a tarball)
docker-pkg/tnv-server
docker-pkg/static/
docker-pkg/data/
*.tar.gz

# Staged deploy bundles (release binary + SPA + data per variant; build output)
/deploy/

# Install-package build output (packaging/make-package.sh)
/dist-pkg/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
